# Intelligent Chatbot Service - environment variables
# Copy to your runtime environment (.env is managed by the platform/orchestrator)

# PostgreSQL connection (env-driven; used by application.properties)
DB_HOST=localhost
DB_PORT=5000
DB_NAME=myapp
DB_USER=appuser
DB_PASSWORD=change-me

# Optional: server port (if needed)
# SERVER_PORT=8080
